Review the following diff from a pull request.

File: {file_path}
Language: {language}
Chunk: {chunk_number} of {total_chunks}

If the total number of chunks is greater than 1, you are seeing a partial view of a larger change. Focus only on this chunk and do not guess about code you cannot see.

The diff is in unified format. Lines starting with + were added, lines starting with - were removed, and lines starting with a space are unchanged context. Hunk headers like @@ -a,b +c,d @@ give the line numbering in the new version of the file.

Diff content:

{diff_content}

Respond with a single JSON object and nothing else, matching exactly this shape:

{{
  "issues": [
    {{
      "file": "{file_path}",
      "line": <line number in the new version of the file, as an integer>,
      "severity": "one of: critical, high, medium, low",
      "category": "one of: bug, security, performance, style, error_handling",
      "title": "short, specific title",
      "explanation": "what the issue is and why it matters",
      "suggestion": "concrete fix, with actual code when possible"
    }}
  ]
}}

Include one object in the "issues" array for every real bug, security problem, performance problem, error-handling gap, or significant style issue in the added or modified lines. If there are no real issues in this chunk, return {{"issues": []}}.
